execution:
- concurrency: 50
    hold-for: 3m
    ramp-up: 2m

    scenario: Choose Flight
  scenarios:
    Choose Flight:
      requests:
      - lable: blazedemo
	method: GET
	url: http://blazedemo.com
      - label: reserve
	method: POST
	url: http//blasedemo.com/reserve.php
	body:
	  fromPort: Paris
	  toPort: Buenos Aires

